# 可选固定数据源示例。复制为 datasources.yaml 后按需启用。
# 推荐通过管理页面维护生产数据源；密码和高权限用户名只能引用环境变量。
datasources: {}

# 示例：
# datasources:
#   analytics:
#     dialect: postgres
#     version: "17"
#     host: analytics-db.internal
#     port: 5432
#     database: analytics
#     username: readonly_user
#     password: ${ANALYTICS_DB_PASSWORD}
#     description: 分析只读库
#     tags: [生产, 只读]
