# 由 pyproject.toml 生成: poetry export -f requirements.txt --output requirements.txt
# 以下为解析后的依赖列表 (开发阶段手动维护)

fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.0
pydantic-settings>=2.0
langchain>=0.3.0
langchain-openai>=0.3.0
langchain-anthropic>=0.3.0
langgraph>=0.2.0
sqlalchemy[asyncio]>=2.0
sqlglot>=25.0
chromadb>=0.5.0
pandas>=2.0
numpy>=2.0
structlog>=24.0
httpx>=0.28.0
pyyaml>=6.0
mcp>=1.0.0
redis>=5.0
sse-starlette>=2.0

# 可选依赖 — 按需取消注释
# clickhouse-connect>=0.8.0
# aiomysql>=0.2.0
# asyncpg>=0.30.0
